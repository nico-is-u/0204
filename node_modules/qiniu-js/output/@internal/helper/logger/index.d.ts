export declare type LogLevel = 'INFO' | 'WARN' | 'ERROR' | 'NONE';
export declare class Logger {
    private logLevel;
    private prefix;
    constructor(logLevel?: LogLevel, prefix?: string);
    private log;
    info(message: string): void;
    warn(message: string): void;
    error(message: string): void;
}
