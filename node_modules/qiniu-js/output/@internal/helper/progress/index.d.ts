declare type ProgressListener = (progress: number) => void;
/** 虚拟进度条；在没有调用 end 之前会无限慢慢逼近 100%，但不会到达 100% */
export declare class MockProgress {
    /** 最大时间；单位为秒 */
    private timeConstant;
    private progress;
    private intervalIds;
    private listeners;
    constructor(
    /** 最大时间；单位为秒 */
    timeConstant?: number);
    private clearInterval;
    private callListeners;
    private setProgress;
    start(): void;
    end(): void;
    stop(): void;
    onProgress(listener: ProgressListener): void;
}
export {};
